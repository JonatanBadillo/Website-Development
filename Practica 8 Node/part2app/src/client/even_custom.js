// Exporta una función llamada Even que toma un parámetro llamado context
export const Even = (context) => `
<h4 class="bg-secondary text-white m-2 p-2">
    <!-- Muestra el valor del contador si es par -->
    Even value: ${context.counter}
</h4>`;
