var sum = 0;
// Declare and initialize the loop variable
for(var num = 1; num <= 6; num = num + 1) {
	// Add the current value of num to sum
	sum = sum + num;
}
alert("The sum of the numbers 1 through 6 is " + sum);

